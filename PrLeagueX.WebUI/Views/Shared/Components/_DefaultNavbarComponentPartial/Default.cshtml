@{
    var path = Context.Request.Path.Value?.ToLower() ?? "";
}

<nav class="nb">
    <div class="nb-inner">
        <a href="/Default/Index" class="brand">
            <div class="brand-ico">⚽</div>
            <span class="brand-txt">PrLeague<span>X</span></span>
        </a>

        <div class="nav-links d-none d-lg-flex">
            <a href="/Default/Index" class="nl @(path == "/" || path.StartsWith("/default") ? "active" : "")">
                Ana Sayfa
            </a>

            <a href="/Fixture/Index" class="nl @(path.StartsWith("/fixture") ? "active" : "")">
                Fikstür
            </a>

            <a href="/Standings/Index" class="nl @(path.StartsWith("/standings") ? "active" : "")">
                Puan Durumu
            </a>
            
        </div>

        <div class="nb-icons">
            <a href="/Team/Index" class="ico-btn d-none d-sm-flex">
                <i class="bi bi-shield"></i>
            </a>

            <a href="/Fixture/Index" class="ico-btn d-none d-sm-flex">
                <i class="bi bi-calendar-event"></i>
            </a>

            <a href="/Admin/Dashboard/Index" class="ico-btn">
                <i class="bi bi-person"></i>
            </a>

            <button class="tog d-lg-none" id="tog" type="button">
                <i class="bi bi-list"></i>
            </button>
        </div>
    </div>

    <div class="mob-menu" id="mobMenu">
        <div class="wrap">
            <a href="/Default/Index" class="nl d-block @(path == "/" || path.StartsWith("/default") ? "active" : "")">
                Ana Sayfa
            </a>

            <a href="/Fixture/Index" class="nl d-block @(path.StartsWith("/fixture") ? "active" : "")">
                Fikstür
            </a>

            <a href="/Standings/Index" class="nl d-block @(path.StartsWith("/standings") ? "active" : "")">
                Puan Durumu
            </a>
            
        </div>
    </div>
</nav>

<script>
    const tog = document.getElementById('tog');
    const mob = document.getElementById('mobMenu');

    if (tog && mob) {
        tog.addEventListener('click', () => {
            mob.classList.toggle('open');

            tog.innerHTML = mob.classList.contains('open')
                ? '<i class="bi bi-x-lg"></i>'
                : '<i class="bi bi-list"></i>';
        });
    }
</script>